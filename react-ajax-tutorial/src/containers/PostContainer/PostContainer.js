import React from 'react';
import { PostWrapper, Navigate } from '../../components';

const PostContainer = () => {
  return (
    <PostWrapper>
      <Navigate />
    </PostWrapper>
  );
};

export default PostContainer;